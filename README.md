# Snowflake.IAC

This slot is reserved for future Snowflake infrastructure-as-code modules (Terraform, native Snowflake objects, and supporting automation). The directory stays empty until those assets are published so existing consumers can plan around the final layout.
